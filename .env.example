# Optional: improves search quality. Without this, BuiltByEcho Research falls back to DuckDuckGo HTML scraping.
BRAVE_API_KEY=

# Optional defaults for local browser profiles / traces.
# BUILT_BY_ECHO_PROFILE_DIR=.profiles
# BUILT_BY_ECHO_TRACE_DIR=output/traces
